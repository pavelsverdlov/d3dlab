<UserControl x:Class="D3DLab.Viewer.Presentation.Componets.WireframeComponetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:D3DLab.Viewer.Presentation.Componets"
             mc:Ignorable="d" >
    <StackPanel Orientation="Vertical" Margin="0,5,0,5">
        <TextBlock Text="Wireframe" Margin="0,0,0,5"
                   Foreground="{StaticResource HighlightedTextColor}"/>
        <ToggleButton Style="{StaticResource ImageToggleButton}"
                      HorizontalAlignment="Stretch" 
                      IsChecked="{Binding IsTransparent}"
                      Command="{Binding TransparentCheckedCommand}">
            <TextBlock Text="Transparent mode" TextAlignment="Center" />
        </ToggleButton>
    </StackPanel>
</UserControl>
