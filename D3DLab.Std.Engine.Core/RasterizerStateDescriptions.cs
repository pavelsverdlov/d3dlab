using System;
using System.Collections.Generic;
using System.Text;

namespace D3DLab.Std.Engine.Core {
    public static class RasterizerStateDescriptions {/*
        public static RasterizerStateDescription GetSolidMeshDescription(CullMode mode) {
            return new RasterizerStateDescription() {
                FillMode = FillMode.Solid,
                CullMode = mode,
                DepthBias = 0,//material.DepthBias,
                DepthBiasClamp = -1000,
                SlopeScaledDepthBias = +0,
                IsDepthClipEnabled = true,
                IsFrontCounterClockwise = true,

                //IsMultisampleEnabled = true,
                //IsAntialiasedLineEnabled = true,                    
                //IsScissorEnabled = true,
            };

        }

        public static RasterizerStateDescription GetLineDescription() {
            return new RasterizerStateDescription() {
                FillMode = FillMode.Solid,
                CullMode = CullMode.None,
                DepthBias = -10,
                DepthBiasClamp = -1000,
                SlopeScaledDepthBias = -1,
                //IsDepthClipEnabled = true,
                IsFrontCounterClockwise = false,

                IsMultisampleEnabled = true,
                IsAntialiasedLineEnabled = true, // Intel HD 3000 doesn't like this (#10051) and it's not needed
                                                 //IsScissorEnabled = true,
            };

        }*/
    }
}
